{
  "private": true,
  "scripts": {
    "build": "node tools/build-single-file.mjs --write",
    "build:check": "node tools/build-single-file.mjs --check",
    "browsers:install": "playwright install chromium",
    "hooks:install": "git config core.hooksPath .githooks",
    "test:query-submit": "playwright test tests/query-submission.spec.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
